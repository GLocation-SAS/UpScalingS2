{
  "name": "process-sentinel-image-function",
  "version": "1.0.0",
  "description": "Google Cloud Function for processing Sentinel-2 TIFF images to JPEG",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=processSentinelImage",
    "dev": "nodemon --exec functions-framework --target=processSentinelImage --watch",
    "deploy": "gcloud run deploy process-sentinel-image --source . --function processSentinelImage --base-image nodejs22 --region us-east1 --allow-unauthenticated --project emuclient "
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "@google-cloud/storage": "^7.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
